To run the script, you are required to install TOR and start tor services as well. Run the script with sudo as sudo python gsmarena_scraper_tor.py.
to run, TOR is needed to bypass late limiting on gsmarena website because we need proxies rotation.
